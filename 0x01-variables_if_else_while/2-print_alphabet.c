#include <stdio.h>

/**
* main - Entry
*
* Return: 0 if successful
*
*/

int main(void)
{
	for (char start = 'a'; start  <= 'z'; start++)
	{
		putchar(start);
		putchar('\n');
		char++;
	}  return (0);
}
